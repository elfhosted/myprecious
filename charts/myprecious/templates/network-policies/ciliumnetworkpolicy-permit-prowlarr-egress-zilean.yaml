{{ if or .Values.prowlarr.enabled .Values.prowlarrplus.enabled .Values.prowlarrhobbit.enabled }}
apiVersion: "cilium.io/v2"
kind: CiliumNetworkPolicy
metadata:
  name: "permit-prowlarr-egress-zilean"
spec:
  endpointSelector:
    matchLabels:
      app.elfhosted.com/name: prowlarr
  egress:
  - toEndpoints:
    - matchLabels:
        app.kubernetes.io/name: zilean
        k8s:io.kubernetes.pod.namespace: zilean
    toPorts:
      - ports:
        - port: '8181'
          protocol: TCP
{{ end }}