{{ if .Values.codeserver.enabled }}
apiVersion: "cilium.io/v2"
kind: CiliumNetworkPolicy
metadata:
  name: "permit-codeserver-egress-torrentio"
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: codeserver
  egress:
  - toEndpoints:
    - matchLabels:
        app.kubernetes.io/name: knightcrawler-addon
        k8s:io.kubernetes.pod.namespace: knightcrawler
    toPorts:
      - ports:
        - port: '7000'
          protocol: TCP
{{ end }}