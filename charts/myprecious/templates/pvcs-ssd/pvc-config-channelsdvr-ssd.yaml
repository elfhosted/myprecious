{{ if .Values.channelsdvr.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-channelsdvr-ssd
  annotations:
    helm.sh/resource-policy: keep
spec:
{{- $pvc := (lookup "v1" "PersistentVolumeClaim" .Release.Namespace "config-channelsdvr-ssd" ) }}
  {{- if and (not $pvc) (.Values.location.enabled) }}
  dataSourceRef:
    kind: ReplicationDestination
    apiGroup: volsync.backube
    name: rd-config-channelsdvr-ssd
  {{- end }}
  accessModes:
  - {{ .Values.storageclass.rwx.accessMode }}
  resources:
    requests:
      storage: 1GMi
  storageClassName: {{ .Values.storageclass.rwx.name }}
{{ end }}