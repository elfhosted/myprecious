apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: storagehub-export
  annotations:
    helm.sh/resource-policy: delete
spec:
  accessModes:
  - {{ .Values.storageclass.rwo.accessMode }}
  resources:
    requests:
      storage: 2Gi
  storageClassName: {{ .Values.storageclass.rwo.name }}
