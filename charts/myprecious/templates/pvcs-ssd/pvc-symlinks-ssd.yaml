apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: symlinks-ssd
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  storageClassName: {{ .Values.storageclass.rwx }}
