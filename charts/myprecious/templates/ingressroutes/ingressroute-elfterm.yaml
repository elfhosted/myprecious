{{ if .Values.filebrowser.enabled }}
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Release.Name }}-elfterm.elfhosted.com
spec:
  routes:
    - match: Host(`{{ .Release.Name }}-elfterm.elfhosted.com`)
      kind: Rule
      services:
        - name: filebrowser
          kind: Service
          port: 3001
      middlewares:
        - name: {{ .Release.Name }}-traefik-forward-auth
        - name: error-pages
          namespace: traefik
        # - name: buffer-2g
        #   namespace: traefik
{{ end }}