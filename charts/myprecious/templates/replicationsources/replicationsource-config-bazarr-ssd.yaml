{{ if .Values.bazarr.enabled }}
{{ if .Values.volsync.enabled }}
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: rs-config-bazarr-ssd
spec:
  sourcePVC: config-bazarr-ssd
  trigger:
    schedule: "0 {{ ( sha256sum .Release.Name| substr 0 8 | regexReplaceAll '[^0-9]' '0' | int ) | mod 24 }} * * *"
  restic:
    pruneIntervalDays: 7
    repository: config-bazarr-ssd-volsync-restic-config
    retain:
      daily: 2
    copyMethod: Snapshot
    cacheCapacity: 1Gi
    cacheStorageClassName: topolvm-provisioner-thin
    cacheAccessModes:
    - ReadWriteOnce
    volumeSnapshotClassName: {{ .Values.storageclass.rwx.volumeSnapshotClassName }}
{{ end }}
{{ end }}