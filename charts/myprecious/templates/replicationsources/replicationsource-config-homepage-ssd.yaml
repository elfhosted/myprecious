{{ if .Values.homepage.enabled }}
{{ if .Values.volsync.enabled }}
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: rs-config-homepage-ssd
spec:
  sourcePVC: config-homepage-ssd
  trigger:
    schedule: "0 9 * * *" # 1h before maintenance
    manual: tbc
  restic:
    pruneIntervalDays: 7
    repository: config-homepage-ssd-volsync-restic-config
    retain:
      daily: 2
    copyMethod: Snapshot
    cacheCapacity: 1Gi
    cacheStorageClassName: topolvm-provisioner-thin
    cacheAccessModes:
    - ReadWriteOnce
    volumeSnapshotClassName: {{ .Values.storageclass.rwx.volumeSnapshotClassName }}
{{ end }}
{{ end }}