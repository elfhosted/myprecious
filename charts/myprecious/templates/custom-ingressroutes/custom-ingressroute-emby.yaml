{{- if .Values.embycustomdomain.enabled }}
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Values.embycustomdomain.cname | lower }}
spec:
  routes:
    - match: Host(`{{ .Values.embycustomdomain.cname | lower }}`)
      kind: Rule
      services:
        - name: emby
          kind: Service
          port: 8096
    - match: Host(`{{ .Values.embycustomdomain.cname | lower }}`) && (PathPrefix(`/video/{something:.+}`) || PathPrefix(`/Items/{something:.+}/Download`))
      kind: Rule
      services:
        - name: emby
          kind: Service
          port: 8096
      middlewares:
        - name: {{ .Release.Name }}-inflightreq-streaming
  tls:
    secretName: cert-{{ .Values.embycustomdomain.cname | lower }}
{{ end }}