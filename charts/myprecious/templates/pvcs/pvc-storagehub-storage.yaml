kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: storagehub
  labels:
    velero.io/exclude-from-backup: "true"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  volumeName: storagehub-{{ .Release.Name }}-storage
  storageClassName: smb