{{ if .Values.riven.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: riven-frontend-env
data:
  PROTOCOL_HEADER: x-forwarded-proto
  HOST_HEADER: x-forwarded-host
  BACKEND_URL: http://riven:8080
  DATABASE_URL: postgres://postgres:postgres@riven/riven
  DIALECT: postgres
{{ end }}