apiVersion: v1
kind: ConfigMap
metadata:
  name: homer-config
data:
  disk_usage.sh: |
    #!/bin/bash

    # # Un-break permissions on config PVCs, if attaching them here has caused a problem
    # chown 568:568 /config/* # if this pod (which runs as root) starts first, it can set config volumes to be owned by root
    
    if [ ! -f /www/assets/backgrounds/background-dark.jpg ]; then
      curl -s 'https://images.unsplash.com/photo-1545486332-9e0999c535b2?ixlib=rb-4.0.3&q=85&fm=jpg&crop=entropy&cs=srgb&dl=adrien-olichon-_GH9LwhlSO4-unsplash.jpg&w=640' -L --output /www/assets/backgrounds/background-dark.jpg
    fi

    if [ ! -f /www/assets/backgrounds/background-light.jpg ]; then
      curl -s 'https://unsplash.com/photos/kO1G3neRA2o/download?ixid=M3wxMjA3fDB8MXxzZWFyY2h8MTh8fGxpZ2h0JTIwYmFja2dyb3VuZHxlbnwwfHx8fDE3MjExNzAzMzd8MA&force=true&w=640' -L --output /www/assets/backgrounds/background-light.jpg
    fi
    
    {{ if .Values.zurg.enabled }}
    # until zurg is up, continue producing this error message every minute
    DAYSREMAINING=$(curl -s http://zurg:9999/stats | jq .user_infos[0].premium)
    while [[ ! "$DAYSREMAINING" -gt 0 ]]; do
      echo "Zurg not up yet, waiting...."
      echo '{"title": "Warning, {{ .Release.Name }}, your setup is incomplete 😱!","content": "⚠️ Zurg is not running, likely you key is not setup, invalid, or expired - Add it <A HREF=https://{{ .Release.Name }}-filebrowser.{{ .Values.dns_domain }}/files/config/zurg/config.yml>here</A> (refreshes every 5min)","style": "is-danger","icon": "fas fa-exclamation-triangle"}' > /www/assets/message/message.json
      sleep 60s  
      DAYSREMAINING=$(curl -s http://zurg:9999/stats | jq .user_infos[0].premium)
    done    
    {{ end }}

    {{ if .Values.decypharr.enabled }}
    # until decypharr is up, continue producing this error message every minute
    DAYSREMAINING=$(curl -s http://decypharr:8282/debug/stats | jq '.debrids[] | select(.name == "realdebrid") | .premium / 86400 | floor')
    while [[ ! "$DAYSREMAINING" -gt 0 ]]; do
      echo "Decypharr not up yet, waiting...."
      echo '{"title": "Warning, {{ .Release.Name }}, your setup is incomplete 😱!","content": "⚠️ Decypharr is not running, likely you key is not setup, invalid, or expired - Add it <A HREF=https://{{ .Release.Name }}-decypharr.{{ .Values.dns_domain }}/config>here</A> (refreshes every 5min)","style": "is-danger","icon": "fas fa-exclamation-triangle"}' > /www/assets/message/message.json
      sleep 60s  
      DAYSREMAINING=$(curl -s http://decypharr:8282/debug/stats | jq '.debrids[] | select(.name == "realdebrid") | .premium / 86400 | floor')
    done    
    {{ end }}    

    rm /www/assets/message/message.json
    while true
    do

      # Once for all of /config, /backup, /logs...
      # tree --du -hF -s  /config/ -H https://{{ .Release.Name }}-filebrowser.{{ .Values.dns_domain }}/files/config -T "🧝 ElfHosted [/config] usage for {{ .Release.Name }}"> /www/assets/message/disk-usage-config.html
      # tree --du -hF -s  /backup/ -H https://{{ .Release.Name }}-filebrowser.{{ .Values.dns_domain }}/files/backup -T "🧝 ElfHosted [/backup] usage for {{ .Release.Name }}"> /www/assets/message/disk-usage-backup.html
      # tree --du -hF -s  /logs/   -H https://{{ .Release.Name }}-filebrowser.{{ .Values.dns_domain }}/files/logs -T "🧝 ElfHosted [/logs] usage for {{ .Release.Name }}"> /www/assets/message/disk-usage-logs.html

      # TOTAL_USED_RAW=$(du -s /config/ | cut -f1)
      # TOTAL_USED_HUMAN=$(du -sh /config/ | cut -f1)
      
      # for STORAGE_PATH in $(ls /config); do
      #   # And again per-app /config...
      #   # disabled on 19 Feb 2024 - consumes too much I/O now that we have "infinite" streaming - @funkypenguin
      #   # tree --du -hF -s  /config/${STORAGE_PATH} -H https://{{ .Release.Name }}-filebrowser.{{ .Values.dns_domain }}/files/config/${STORAGE_PATH} -T "🧝 ElfHosted [/config/${STORAGE_PATH}] usage for {{ .Release.Name }}"> /www/assets/message/disk-usage-config-${STORAGE_PATH}.html

      #   USED_RAW=$(du -s /config/${STORAGE_PATH} | cut -f1)
      #   USED_HUMAN=$(du -sh /config/${STORAGE_PATH} | cut -f1)
        
      #   # Keep the highest percentage used number
      #   if [[ ! -z ${EXISTING_USAGE+x} ]]; then
      #     if [[ "$USED_RAW" -gt "$EXISTING_USAGE" ]]; then
      #       EXISTING_USAGE=$USED_RAW
      #       CONFIG_BIGGEST_CONSUMER=$STORAGE_PATH
      #       CONFIG_DISK_USED_HUMAN=$USED_HUMAN
      #       echo -n "<A HREF='https://{{ .Release.Name }}-filebrowser.{{ .Values.dns_domain }}/files/config/' TARGET='_blank'>/config</A>: Largest volume-per-app consumer is <A HREF='https://{{ .Release.Name }}.{{ .Values.dns_domain }}/assets/message/disk-usage-config-${CONFIG_BIGGEST_CONSUMER}/' TARGET='_blank'>${CONFIG_BIGGEST_CONSUMER}</A> with ${CONFIG_DISK_USED_HUMAN} used<BR/>" > /www/assets/message/message_details_config.html
      #     fi
      #   else
      #       EXISTING_USAGE=$USED_RAW
      #       CONFIG_BIGGEST_CONSUMER=$STORAGE_PATH
      #       CONFIG_DISK_USED_HUMAN=$USED_HUMAN
      #       echo -n "<A HREF='https://{{ .Release.Name }}-filebrowser.{{ .Values.dns_domain }}/files/config/' TARGET='_blank'>/config</A>: Largest volume-per-app consumer is <A HREF='https://{{ .Release.Name }}.{{ .Values.dns_domain }}/assets/message/disk-usage-config-${CONFIG_BIGGEST_CONSUMER}/' TARGET='_blank'>${CONFIG_BIGGEST_CONSUMER}</A> with ${CONFIG_DISK_USED_HUMAN} used<BR/>" > /www/assets/message/message_details_config.html
      #   fi
      # done

      # echo "💾 /config/ $TOTAL_USED_HUMAN of 100GB used: "> /www/assets/message/message_details.html

      # PERCENTAGE_USED=$(cat /www/assets/message/disk_space_percent_used)
      # if [[ "$PERCENTAGE_USED" -ge 90 || "$CONFIG_PERCENTAGE_USED" -ge 90 ]]; then
      #   STYLE="is-danger"
      #   ICON="fa fa-thermometer-full"
      #   MESSAGE="Time to clean up! 🧹"
      # elif [[ "$PERCENTAGE_USED" -ge 75 || "$CONFIG_PERCENTAGE_USED" -ge 75 ]]; then
      #   STYLE="is-warning"
      #   ICON="fa fa-thermometer-three-quarters"
      #   MESSAGE="Getting full.. 🍔"
      # elif [[ "$PERCENTAGE_USED" -ge 50 || "$CONFIG_PERCENTAGE_USED" -ge 50 ]]; then
      #   STYLE="is-info"
      #   ICON="fa fa-thermometer-half"
      #   MESSAGE="Halfway there.."
      # elif [[ "$PERCENTAGE_USED" -ge 25  || "$CONFIG_PERCENTAGE_USED" -ge 25 ]]; then
      #   STYLE="is-success"
      #   ICON="fa fa-thermometer-quarter"
      #   MESSAGE="Nice and tidy! 👍"
      # else
      #   STYLE="is-success"
      #   ICON="fa fa-thermometer-empty"
      #   MESSAGE="Gigabytes everywhere! 🥳"
      # fi

      {{ if .Values.zurg.enabled }}
      RD_DAYS_REMAINING=$(curl -s http://zurg:9999/stats | jq -r '[.user_infos[] | "\(.username) expires in \(.premium) days"] | join(", ")')
      RD_LIBRARY_SIZE=$(curl -s http://zurg:9999/stats | jq .library_size) # items
      RD_MESSAGE="👽 Zurg reports $RD_LIBRARY_SIZE items, $RD_DAYS_REMAINING"
      {{ end }}

      {{ if .Values.decypharr.enabled }}
      RD_USERNAME=$(curl -s http://decypharr:8282/debug/stats | jq '.debrids[] | select(.name == "realdebrid") | .username' -r)
      RD_POINTS=$(curl -s http://decypharr:8282/debug/stats | jq '.debrids[] | select(.name == "realdebrid") | .points')
      RD_DAYS_REMAINING=$(curl -s http://decypharr:8282/debug/stats | jq '.debrids[] | select(.name == "realdebrid") | .premium / 86400 | floor')
      RD_LIBRARY_SIZE=$(curl -s http://decypharr:8282/debug/stats | jq '.debrids[] | select(.name == "realdebrid") | .library_size')
      RD_BAD_TORRENTS=$(curl -s http://decypharr:8282/debug/stats | jq '.debrids[] | select(.name == "realdebrid") | .bad_torrents')
      RD_MESSAGE="🤖 Decypharr reports $RD_LIBRARY_SIZE items ($RD_BAD_TORRENTS bad) for user $RD_USERNAME, with $RD_DAYS_REMAINING days remaining, and $RD_POINTS points!"
      {{ end }}      

      {{ if .Values.riven.enabled }}
      RIVEN_TOTAL_ITEMS=$(curl -s -H "Authorization: Bearer {{ .Values.rivenexposed.apikey }}" http://riven:8080/api/v1/stats | jq .total_items) # items
      RIVEN_TOTAL_MOVIES=$(curl -s -H "Authorization: Bearer {{ .Values.rivenexposed.apikey }}" http://riven:8080/api/v1/stats | jq .total_movies) # items
      RIVEN_TOTAL_SHOWS=$(curl -s -H "Authorization: Bearer {{ .Values.rivenexposed.apikey }}" http://riven:8080/api/v1/stats | jq .total_shows) # items
      RIVEN_TOTAL_SEASONS=$(curl -s -H "Authorization: Bearer {{ .Values.rivenexposed.apikey }}" http://riven:8080/api/v1/stats | jq .total_seasons) # items
      RIVEN_TOTAL_EPISODES=$(curl -s -H "Authorization: Bearer {{ .Values.rivenexposed.apikey }}" http://riven:8080/api/v1/stats | jq .total_episodes) # items
      RIVEN_TOTAL_SYMLINKS=$(curl -s -H "Authorization: Bearer {{ .Values.rivenexposed.apikey }}" http://riven:8080/api/v1/stats | jq .total_symlinks) # items
      RIVEN_MESSAGE="🗻 Riven reports $RIVEN_TOTAL_ITEMS total items (<I>$RIVEN_TOTAL_MOVIES movies 🍿, $RIVEN_TOTAL_SHOWS shows 📺 across $RIVEN_TOTAL_SEASONS seasons and $RIVEN_TOTAL_EPISODES episodes across $RIVEN_TOTAL_SYMLINKS symlinks!</I>)"
      {{ end }}

      COMPLIMENT=$(curl -s http://elfhosted-internal.my-fun-api/compliment | jq -r .data.compliment)

      # DISK_USAGE_CONFIG=$(cat /www/assets/message/message_details.html /www/assets/message/message_details_config.html )
      echo "{ \"title\":\"Welcome, {{ .Release.Name }}!\", \"content\":\"❤️ ${COMPLIMENT}</BR>${RD_MESSAGE}<BR/> ${RIVEN_MESSAGE}\",\"style\":\"${STYLE}\",\"icon\":\"${ICON}\" }" > /www/assets/message/message.json
            
      sleep $(shuf -i 1800-5400 -n 1)
    done

  config.yml: |
    ---
    # Homepage configuration
    # See https://fontawesome.com/icons for icons options

    # Optional: Use external configuration file.
    # Using this will ignore remaining config in this file
    # externalConfig: https://example.com/server-luci/config.yaml

    subtitle: "{{ .Release.Name }} is ..."
    title: "ElfHosted!"
    documentTitle: "ElfHosted - {{ .Release.Name }}" # Customize the browser tab text
    # Alternatively a fa icon can be provided:
    # icon: "fas fa-wand-magic-sparkles"
    logo: https://elfhosted.com/images/logo.png
    header: true # Set to false to hide the header
    footer: Version {{ .Chart.Version }}

    columns: "3" # "auto" or number (must be a factor of 12: 1, 2, 3, 4, 6, 12)
    connectivityCheck: false # whether you want to display a message when the apps are not accessible anymore (VPN disconnected for example)

    # Optional theming
    # theme: default # 'default' or one of the themes available in 'src/assets/themes'.

    # Optional custom stylesheet
    # Will load custom CSS files. Especially useful for custom icon sets.
    stylesheet:
      - "assets/custom.css"

    # Here is the exhaustive list of customization parameters
    # However all value are optional and will fallback to default if not set.
    # if you want to change only some of the colors, feel free to remove all unused key.
    colors:
      light:
        highlight-primary: "#222526"
        highlight-secondary: "#4285f4"
        highlight-hover: "#5a95f5"
        background: "#222526"
        card-background: "#ffffff"
        text: "#363636"
        text-header: "#ffffff"
        text-title: "#303030"
        text-subtitle: "#424242"
        card-shadow: rgba(0, 0, 0, 0.1)
        link-hover: "#363636"
        background-image: /assets/backgrounds/background-light.jpg
      dark:
        highlight-primary: "#222526"
        highlight-secondary: "#4285f4"
        highlight-hover: "#5a95f5"
        background: "#222526"
        card-background: "#2b2b2b"
        text: "#eaeaea"
        text-header: "#ffffff"
        text-title: "#fafafa"
        text-subtitle: "#f5f5f5"
        card-shadow: rgba(0, 0, 0, 0.4)
        link-hover: "#ffdd57"
        background-image: /assets/backgrounds/background-dark.jpg

    # Optional message
    message:
      url: https://{{ .Release.Name }}.{{ .Values.dns_domain }}/assets/message/message.json
      # mapping:
      #   content: 'message'
      #   style: 'style'
      #   title: 'title'
      refreshInterval: 10000
      icon: "fas fa-door-open"
    
    # Optional navbar
    # links: [] # Allows for navbar (dark mode, layout, and search) without any links
    links:

      - name: "Apps"
        icon: "fas fa-couch"
        url: "#config"

      - name: "Health"
        icon: "fas fa-heartbeat"
        url: "https://{{ .Release.Name }}-health.{{ .Values.dns_domain }}"
        target: "_blank" # optional html tag target attribute

      - name: "Store"
        icon: "fas fa-store"
        url: "https://store.elfhosted.com"
        target: "_blank" # optional html tag target attribute

      - name: "Docs"
        icon: "fas fa-book"
        url: "https://docs.elfhosted.com"
        target: "_blank" # optional html tag target attribute

      - name: "Account"
        icon: "fa fa-user"
        url: "https://store.elfhosted.com/my-account/"
        target: "_blank" # optional html tag target attribute

      - name: "Discord"
        icon: "fa-brands fa-discord"
        url: "https://discord.elfhosted.com"
        target: "_blank" # optional html tag target attribute

      - name: "Logout"
        icon: "fas fa-lock"
        url: https://{{ .Release.Name }}-auth-me.{{ .Values.dns_domain }}/_oauth/logout

    # Services
    # First level array represents a group.
    # Leave only a "items" key if not using group (group name, icon & tagstyle are optional, section separation will not be displayed).
    services:
      - name: "Consume Media"
        icon: "fas fa-couch"
        # A path to an image can also be provided. Note that icon take precedence if both icon and logo are set.
        # logo: "path/to/logo"
        items:

          {{ if .Values.aiostreams.enabled }}
          - name: "AIOStreams"
            logo: "assets/png/aiostreams.png"
            subtitle: "Combine and format multiple Stremio addons"
            {{ if .Values.aiostreamsexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-aiostreams.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.audiobookshelf.enabled }}
          - name: "Audiobookshelf"
            logo: "assets/png/audiobookshelf.png"
            # Alternatively a fa icon can be provided:
            # icon: "fab fa-photo-video"
            subtitle: "Listen to Books/Podcasts"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-audiobookshelf.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.booklore.enabled }}
          - name: "BookLore"
            logo: "assets/png/booklore.png"
            subtitle: "Manage PDFs / Books"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-booklore.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}          

          {{ if .Values.calibre.enabled }}
          - name: "Calibre"
            logo: "assets/png/calibre.png"
            # Alternatively a fa icon can be provided:
            # icon: "fab fa-photo-video"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-calibre.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.calibreweb.enabled }}
          - name: "Calibre Web"
            logo: "assets/png/calibre-web.png"
            # Alternatively a fa icon can be provided:
            # icon: "fab fa-photo-video"
            subtitle: "Read Books"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-calibre-web.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.comet.enabled }}
          - name: "Comet"
            icon: "fas fa-meteor"
            {{ if .Values.prowlarr.enabled }}            
            subtitle: "Proxy-Stremio from DMM & Jackett"
            {{ else if .Values.prowlarr.enabled }}            
            subtitle: "Proxy-Stremio from DMM & Prowlarr"
            {{ else }}            
            subtitle: "Proxy-Stremio from DMM"
            {{ end }}
            {{ if .Values.cometexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-comet.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}                     

          {{ if .Values.cwabookdownloader.enabled }}
          - name: "CWA Book Downloadoer"
            logo: "assets/png/calibre-web-automated-book-downloader.png"
            subtitle: "Download Books"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-cwa-book-downloader.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}         

          {{ if .Values.davio.enabled }}
          - name: "Davio"
            logo: "assets/png/webdav.png"
            subtitle: "Stremio Addon to stream your WebDAV content"
            {{ if .Values.davioexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-davio.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}       

          {{ if or .Values.emby.enabled .Values.embyranger.enabled .Values.embyhobbit.enabled .Values.embyhalfling.enabled .Values.embynazgul.enabled }}
          - name: "Emby"
            logo: "assets/png/emby.png"
            # Alternatively a fa icon can be provided:
            # icon: "fab fa-photo-video"
            subtitle: "Watch Movies/TV"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-emby.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.flixio.enabled }}
          - name: "Flixio"
            logo: "assets/png/stremio.png"
            subtitle: "Stremio (Anonymous)"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-flixio.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}             

          {{ if .Values.jackettio.enabled }}
          - name: "Jackettio"
            logo: "assets/png/jackett.png"
            subtitle: "Stremio Addon supporting private trackers"
            {{ if .Values.jackettioexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-jackettio.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}      

          {{ if or .Values.jellyfin.enabled .Values.jellyfinranger.enabled .Values.jellyfinhobbit.enabled .Values.jellyfinhalfling.enabled .Values.jellyfinnazgul.enabled  }}
          - name: "Jellyfin"
            logo: "assets/png/jellyfin.png"
            # Alternatively a fa icon can be provided:
            # icon: "fab fa-photo-video"
            subtitle: "Watch Movies/TV"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-jellyfin.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.kavita.enabled }}
          - name: "Kavita"
            logo: "assets/png/kavita.png"
            # Alternatively a fa icon can be provided:
            # icon: "fab fa-photo-video"
            subtitle: "Read Comics / Books"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-kavita.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.komga.enabled }}
          - name: "Komga"
            logo: "assets/png/komga.png"
            # Alternatively a fa icon can be provided:
            # icon: "fab fa-photo-video"
            subtitle: "Read Comics / Books"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-komga.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.mediafusion.enabled }}
          - name: "MediaFusion"
            icon: "fas fa-atom"
            subtitle: "Multi-source AddOn for Stremio"
            {{ if .Values.mediafusionexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-mediafusion.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}  

          {{ if .Values.mediaflowproxy.enabled }}
          - name: "MediaFlow Proxy"
            icon: "fas fa-copy"
            subtitle: "Video Streaming Proxy"
            tag: "protected"
            tagstyle: "is-success"
            url: https://{{ .Release.Name }}-mediaflow-proxy.{{ .Values.dns_domain }}
            target: "_blank" # optional html tag target attribute
          {{ end }}        

          {{ if .Values.navidrome.enabled }}
          - name: "Navidrome"
            logo: "assets/png/navidrome.png"
            # Alternatively a fa icon can be provided:
            # icon: "fab fa-photo-video"
            subtitle: "Listen to Music"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-navidrome.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.nuviostreams.enabled }}
          - name: "Nuvio Streams"
            logo: "assets/png/nuviostreams.png"
            subtitle: "HTTP Streams in Stremio"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-nuviostreams.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}             
                    

          {{ if or .Values.plex.enabled .Values.plexranger.enabled .Values.plexhobbit.enabled .Values.plexhalfling.enabled .Values.plexnazgul.enabled }}
          - name: "Plex"
            logo: "assets/png/plex.png"
            # Alternatively a fa icon can be provided:
            # icon: "fab fa-photo-video"
            subtitle: "Watch Movies/TV"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-plex.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}          

          {{ if .Values.storyteller.enabled }}
          - name: "StoryTeller"
            icon: "fas fa-book-open"
            subtitle: "Listen/Read books"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-storyteller.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}      
            
          {{ if .Values.stremthru.enabled }}
          - name: "StremThru"
            icon: "fas fa-tools"
            subtitle: "Stremio / Debrid Swiss-Army Knife"
            {{ if .Values.stremthruexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-stremthru.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}                

          {{ if .Values.webstreamr.enabled }}
          - name: "WebStreamr"
            icon: "fas fa-tv"
            subtitle: "Watch HTTP streams with Stremio"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-webstreamr.elfhosted.com"
            target: "_blank" # optional html tag target attribute
          {{ end }}   

          {{ if .Values.youriptv.enabled }}
          - name: "YourIPTV"
            icon: "fas fa-tv"
            subtitle: "Watch your IPTV in Stremio"
            {{ if .Values.youriptvexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-youriptv.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}               

                        
      - name: "Manage Media"
        icon: "fas fa-photo-video"
        items:

          {{ if .Values.bazarr.enabled }}
          - name: "Bazarr"
            logo: "assets/png/bazarr.png"
            subtitle: "Manage Subtitles"
            {{ if .Values.bazarrexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-bazarr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.bazarr4k.enabled }}
          - name: "Bazarr 4K"
            logo: "assets/png/bazarr.png"
            subtitle: "Manage Subtitles (4K)"
            {{ if .Values.bazarr4kexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-bazarr4k.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.channelsdvr.enabled }}
          - name: "Channels DVR"
            logo: "assets/png/channels-dvr.png"
            subtitle: "Watch TV your way"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-channelsdvr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}   

          {{ if .Values.cleanuparr.enabled }}
          - name: "Cleanuparr"
            icon: "fas fa-broom"
            subtitle: "Clean Aarr queues"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-cleanuparr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute          
          {{ end }}            

          {{ if or .Values.zurgling.enabled .Values.debridling.enabled .Values.seerrbridge.enabled .Values.cinesync.enabled }}
          - name: "DebridMediaManager"
            logo: "assets/png/debridmediamanager.png"
            subtitle: "Manage Debrid Media"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://debridmediamanager.com"
            target: "_blank" # optional html tag target attribute
          {{ end }}          

          {{ if .Values.decluttarr.enabled }}
          - name: "Decluttarr"
            icon: "fas fa-broom"
            subtitle: "Clean Aarr queues"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-decluttarr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute          
          {{ end }}              

          {{ if .Values.discovarr.enabled }}
          - name: "Discovarr"
            icon: "fas fa-infinity"
            subtitle: "AI Content Discovery"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-discovarr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}     

          {{ if .Values.ersatztv.enabled }}
          - name: "ersatztv"
            icon: "fas fa-tv"
            subtitle: "Live TV Proxy for Plex/Emby"
            {{ if .Values.tunarrexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-ersatztv.{{ .Values.dns_domain }}/web/"
            target: "_blank" # optional html tag target attribute
          {{ end }}    

          {{ if .Values.filebot.enabled }}
          - name: "FileBot"
            logo: "assets/png/filebot.png"
            # Alternatively a fa icon can be provided:
            # icon: "fab fa-photo-video"
            subtitle: "Automate Media Management"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-filebot.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}          

          {{ if .Values.imagemaid.enabled }}
          - name: "ImageMaid"
            icon: "fas fa-broom"
            subtitle: "Clean images in Plex"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-imagemaid.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}             

          {{ if .Values.immich.enabled }}
          - name: "Immich"
            logo: "assets/png/immich.png"
            subtitle: "Manage Photos"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-immich.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}  

          {{ if .Values.jellyseerr.enabled }}
          - name: "Jellyseerr"
            logo: "assets/png/jellyseerr.png"
            subtitle: "Manage Requests"
            tag: "public"
            tagstyle: "is-danger"
            {{- if .Values.jellyseerrcustomdomain.enabled }}
            url: "https://{{ .Values.jellyseerrcustomdomain.cname | lower }}"
            {{- else }}
            url: "https://{{ .Release.Name }}-jellyseerr.{{ .Values.dns_domain }}"
            {{- end }}
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.jellystat.enabled }}
          - name: "Jellystat"
            logo: "assets/png/jellystat.png"
            subtitle: "Jellyfin Statistics"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-jellystat.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}               

          {{ if .Values.jfa.enabled }}
          - name: "Jellyfin Accounts"
            icon: "fas fa-users"
            subtitle: "Manage Jellyfin Accounts / Invites"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-jfa.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.kapowarr.enabled }}
          - name: "Kapowarr"
            icon: "fas fa-comments"
            subtitle: "Manage Comics"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-kapowarr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}         

          {{ if .Values.kometa.enabled }}
          - name: "Kometa"
            icon: "fas fa-meteor"
            # Alternatively a fa icon can be provided:
            # icon: "fas fa-photo-film"
            subtitle: "Manage Plex Metadata"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-kometa.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.lazylibrarian.enabled }}
          - name: "LazyLibrarian"
            logo: "assets/png/lazylibrarian.png"
            subtitle: "Manage Books"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-lazylibrarian.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
            {{ end }}

          {{ if .Values.letterboxdtraktsync.enabled }}
          - name: "Letterboxd Trakt Sync"
            icon: "fas fa-sync"
            subtitle: "Sync Letterboxd and Trakt"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-letterboxd-trakt-sync.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}                  

          {{ if .Values.lidarr.enabled }}
          - name: "Lidarr"
            logo: "assets/png/lidarr.png"
            subtitle: "Manage Music"
            {{ if .Values.lidarrexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-lidarr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
            type: "Lidarr"
            apikey: "{{ .Values.lidarrexposed.apikey }}"
            useCredentials: true
          {{ end }}

          {{ if .Values.mylar.enabled }}
          - name: "Mylar"
            logo: "assets/png/mylar.png"
            subtitle: "Manage Comics"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-mylar.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}       

          {{ if .Values.notifiarr.enabled }}
          - name: "Notifiarr"
            logo: "assets/png/notifiarr.png"
            # Alternatively a fa icon can be provided:
            # icon: "fab fa-photo-video"
            subtitle: "Discord-based notifications"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-notifiarr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}             

          {{ if .Values.ombi.enabled }}
          - name: "Ombi"
            logo: "assets/png/ombi.png"
            subtitle: "Manage Requests"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-ombi.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.overseerr.enabled }}
          - name: "Overseerr"
            logo: "assets/png/overseerr.png"
            subtitle: "Manage Requests"
            tag: "public"
            tagstyle: "is-danger"
            {{- if .Values.overseerrcustomdomain.enabled }}
            url: "https://{{ .Values.overseerrcustomdomain.cname | lower }}"
            {{- else }}
            url: "https://{{ .Release.Name }}-overseerr.{{ .Values.dns_domain }}"
            {{- end }}
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.petio.enabled }}
          - name: "Petio"
            logo: "assets/png/petio.png"
            subtitle: "Manage Requests"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-petio.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}      

          {{ if .Values.plexdebrid.enabled }}
          - name: "plex-debrid (RealDebrid)"
            logo: "assets/png/real-debrid.png"
            subtitle: "Sync Plex / Jellyfin with RealDebrid"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-plex-debrid.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}   

          {{ if .Values.plextraktsync.enabled }}
          - name: "PlexTraktSync"
            icon: "fas fa-sync"
            subtitle: "Sync Plex and Trakt (watch mode)"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-plextraktsync.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.plexytrack.enabled }}
          - name: "PlexyTrack"
            logo: "assets/png/plexytrack.png"
            subtitle: "Sync Plex and Trakt/Simkl"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-plexytrack.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}                         

          {{ if .Values.profilarr.enabled }}
          - name: "Profilarr"
            icon: "fas fa-user"
            subtitle: "Manage Arr Profiles"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-profilarr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}     

          {{ if .Values.radarr.enabled }}
          - name: "Radarr"
            logo: "assets/png/radarr.png"
            subtitle: "Manage Movies"
            {{ if .Values.radarrexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-radarr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
            type: "Radarr"
            apikey: "{{ .Values.radarrexposed.apikey }}"
            useCredentials: true
          {{ end }}

          {{ if .Values.radarr4k.enabled }}
          - name: "Radarr 4K"
            logo: "assets/png/radarr-4k.png"
            subtitle: "Manage Movies (4K)"
            {{ if .Values.radarr4kexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-radarr4k.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
            type: "Radarr"
            apikey: "{{ .Values.radarr4kexposed.apikey }}"
            useCredentials: true
          {{ end }}

          {{ if .Values.readarr.enabled }}
          - name: "Readarr"
            logo: "assets/png/readarr.png"
            subtitle: "Manage Books"
            {{ if .Values.readarrexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-readarr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
            type: "Readarr"
            apikey: "{{ .Values.readarrexposed.apikey }}"
            useCredentials: true
          {{ end }}

          {{ if .Values.readarraudio.enabled }}
          - name: "Readarr (Audio)"
            logo: "assets/png/readarr.png"
            subtitle: "Manage Audio Books"
            {{ if .Values.readarraudioexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-readarraudio.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
            type: "Readarr"
            apikey: "{{ .Values.readarraudioexposed.apikey }}"
            useCredentials: true
          {{ end }}          

          {{ if .Values.recyclarr.enabled }}
          - name: "Recyclarr"
            icon: "fas fa-recycle"
            subtitle: "Configure Recyclarr"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-filebrowser.{{ .Values.dns_domain }}/files/config/recyclarr/"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.riven.enabled }}
          - name: "Riven"
            icon: "fas fa-mountain"
            subtitle: "All-in-one Debrid Media Management"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-riven.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute

          - name: "Riven (backend)"
            icon: "fas fa-mountain"
            subtitle: "Geek out over Riven's backend"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-riven-backend.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}             

          {{ if .Values.rpdb.enabled }}
          - name: "RPDB"
            icon: "fas fa-star"
            subtitle: "Manage Media Ratings Posters"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-rpdb.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.shoko.enabled }}
          - name: "Shoko"
            logo: "assets/png/shoko.png"
            # Alternatively a fa icon can be provided:
            # icon: "fab fa-photo-video"
            subtitle: "Manage Anime"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-shoko.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.sonarr.enabled }}
          - name: "Sonarr"
            logo: "assets/png/sonarr.png"
            subtitle: "Manage Series"
            {{ if .Values.sonarrexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-sonarr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
            type: "Sonarr"
            apikey: "{{ .Values.sonarrexposed.apikey }}"
            useCredentials: true
          {{ end }}

          {{ if .Values.sonarr4k.enabled }}
          - name: "Sonarr 4K"
            logo: "assets/png/sonarr-4k.png"
            subtitle: "Manage Series (4K)"
            {{ if .Values.sonarr4kexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-sonarr4k.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
            type: "Sonarr"
            apikey: "{{ .Values.sonarr4kexposed.apikey }}"
            useCredentials: true
          {{ end }}

          {{ if .Values.suggestarr.enabled }}
          - name: "suggestarr"
            icon: "fas fa-infinity"
            subtitle: "Automate Requests"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-suggestarr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}  

          {{ if .Values.symlinkcleaner.enabled }}
          - name: "Symlink Cleaner"
            icon: "fas fa-broom"
            subtitle: "Manage Symlinks"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-symlink-cleaner.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.tautulli.enabled }}
          - name: "Tautulli"
            logo: "assets/png/tautulli.png"
            # Alternatively a fa icon can be provided:
            # icon: "fab fa-photo-video"
            subtitle: "Plex Statistics"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-tautulli.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.threadfin.enabled }}
          - name: "Threadfin"
            # Alternatively a fa icon can be provided:
            icon: "fas fa-tv"
            subtitle: "Live TV Proxy for Plex/Jellyfin/Emby"
            {{ if .Values.threadfinexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-threadfin.{{ .Values.dns_domain }}/web/"
            target: "_blank" # optional html tag target attribute
          {{ end }}  

          {{ if .Values.tunarr.enabled }}
          - name: "tunarr"
            icon: "fas fa-tv"
            subtitle: "Live TV Proxy for Plex/Emby"
            {{ if .Values.tunarrexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-tunarr.{{ .Values.dns_domain }}/web/"
            target: "_blank" # optional html tag target attribute
          {{ end }}


          {{ if .Values.wizarr.enabled }}
          - name: "Wizarr"
            logo: "assets/png/wizarr.png"
            subtitle: "Manage Plex / Emby / Jellyfin Accounts / Invites"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-wizarr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}          

      - name: "Download Media"
        icon: "fas fa-cloud-arrow-down"
        items:           

          {{ if .Values.audiobookbayautomated.enabled }}
          - name: "AudioBookBay Automated"
            icon: "fas fa-book-open-reader"
            subtitle: "Manage AudioBookBay Downloads"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-audiobookbay-automated.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}             

          {{ if .Values.audiobookrequest.enabled }}
          - name: "AudioBookRequest"
            icon: "fas fa-book-open-reader"
            subtitle: "Manage AudioBook Downloads"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-audiobookrequest.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.autopulse.enabled }}
          - name: "Autopulse"
            logo: "assets/png/autopulse.png"            
            subtitle: "Trigger Streamer Scans"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-autopulse.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}    

          {{ if or .Values.airdcpppia.enabled  .Values.airdcppgluetun.enabled }}
          - name: "AirDC++"
            icon: "fas fa-wind"
            subtitle: "Manage DC++ Sharing"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-airdcpp.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}    

          {{ if .Values.cinesync.enabled }}
          - name: "CineSync"
            logo: "assets/png/cinesync.png"
            subtitle: "Symlink / sort debrid media"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-cinesync.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute            
          {{ end }}  

          {{ if .Values.clidebrid.enabled }}
          - name: "cli-debrid (RealDebrid)"
            logo: "assets/png/real-debrid.png"
            subtitle: "Sync Plex with RealDebrid (with a UI)"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-cli-debrid.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}            

          {{ if .Values.debridav.enabled }}
          - name: "DebriDav"
            icon: "fas fa-file-export"
            subtitle: "DebriDav"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-filebrowser.{{ .Values.dns_domain }}/files/debridav/"
            target: "_blank" # optional html tag target attribute
          {{ end }}  

          {{ if .Values.decypharr.enabled }}
          - name: "Decypharr"
            icon: "fas fa-circle"
            subtitle: "Debrid Blackhole"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-decypharr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}      

          {{ if or .Values.doplarr.enabled }}
          - name: "Doplarr"
            icon: "fas fa-comments"
            subtitle: "Manage media requests via Discord"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-doplarr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}             

          {{ if .Values.huntarr.enabled }}
          - name: "Huntarr"
            icon: "fas fa-bullseye"
            subtitle: "Hunt Missing Movies / Episodes"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-huntarr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}   

          {{ if .Values.jackett.enabled }}
          - name: "Jackett"
            logo: "assets/png/jackett.png"
            subtitle: "Manage Indexers / Trackers"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-jackett.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}    

          {{ if .Values.listsync.enabled }}
          # - name: "ListSync"
          #   icon: "fas fa-sync"
          #   {{ if .Values.jellyseerr.enabled }}
          #   subtitle: "Sync Jellyseer with Lists"
          #   {{ else }}
          #   subtitle: "Sync Overseerr with Lists"
          #   {{ end }}
          #   tag: "protected"
          #   tagstyle: "is-success"
          #   url: "https://{{ .Release.Name }}-listsync.{{ .Values.dns_domain }}"
          #   target: "_blank" # optional html tag target attribute  

          - name: "ListSync CLI"
            icon: "fas fa-terminal"
            {{ if .Values.jellyseerr.enabled }}
            subtitle: "Sync Jellyseer with Lists (CLI)"
            {{ else }}
            subtitle: "Sync Overseerr with Lists (CLI)"
            {{ end }}
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-listsync-cli.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute                                    
          {{ end }}      

          {{ if .Values.nzbget.enabled }}
          - name: "NZBGet"
            logo: "assets/png/nzbget.png"
            subtitle: "Usenet client"
            {{ if .Values.nzbgetexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-nzbget.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.nzbhydra.enabled }}
          - name: "NZBHydra"
            logo: "assets/png/nzbhydra.png"
            subtitle: "Manage NZB Indexers"
            {{ if .Values.nzbhydra.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-nzbhydra.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}          

          {{ if .Values.openbooks.enabled }}
          - name: "Openbooks"
            icon: "fas fa-book-open-reader"
            subtitle: "Manage Book Downloads"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-openbooks.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }} 

          {{ if .Values.prowlarr.enabled }}
          - name: "Prowlarr"
            logo: "assets/png/prowlarr.png"
            subtitle: "Manage Indexers"
            {{ if .Values.prowlarrexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-prowlarr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
            type: "Prowlarr"
            apikey: "{{ .Values.prowlarrexposed.apikey }}"
            useCredentials: true
          {{ end }}     

          {{ if or .Values.pulsarr.enabled }}
          - name: "Pulsarr"
            icon: "fas fa-star"
            subtitle: "Add Plex watchlisted items to Aars"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-pulsarr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}                                 

          {{ if or .Values.qbittorrentpia.enabled .Values.qbittorrentgluetun.enabled }}
          - name: "Qbittorrent"
            logo: "assets/png/qbittorrent.png"
            subtitle: "Bittorrent client"
            {{ if .Values.qbittorrentexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-qbittorrent.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
            type: "qBittorrent"
            rateInterval: 30000 # Interval for updating the download and upload rates.
            torrentInterval: 60000 # Interval for updating the torrent count.
            useCredentials: true
            {{ end }}

          {{ if .Values.rdtclient.enabled }}
          - name: "RDT Client (RealDebrid)"
            logo: "assets/png/real-debrid.png"
            subtitle: "Manage Debrid Downloads"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-rdtclient.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}  

          {{ if or .Values.requestrr.enabled }}
          - name: "Requestrr"
            icon: "fas fa-comments"
            subtitle: "Manage media requests via Chatbot"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-requestrr.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}             

          {{ if or .Values.rutorrentpia.enabled .Values.rutorrentgluetun.enabled }}
          - name: "ruTorrent"
            logo: "assets/png/rutorrent.png"
            subtitle: "Bittorrent client"
            {{ if .Values.rutorrentexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-rutorrent.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
            # xmlrpc: "https://{{ .Release.Name }}-rutorrent.{{ .Values.dns_domain }}" # Homer will automatically suffix /RPC2 to this, which we catch in our ingressroute.
            # type: "Rtorrent"
            # rateInterval: 30000 # Interval for updating the download and upload rates.
            # torrentInterval: 60000 # Interval for updating the torrent count.
            # useCredentials: true
            {{ end }}

          {{ if .Values.sabnzbd.enabled }}
          - name: "SABnzbd"
            logo: "assets/png/sabnzbd.png"
            subtitle: "Usenet client"
            {{ if .Values.sabnzbdexposed.enabled }}
            tag: "public"
            tagstyle: "is-danger"
            {{ else }}
            tag: "protected"
            tagstyle: "is-success"
            {{ end }}
            url: "https://{{ .Release.Name }}-sabnzbd.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
            type: "SABnzbd"
            apikey: "{{ .Values.sabnzbdexposed.apikey }}"
            downloadInterval: 60000 # (Optional) Interval (in ms) for updating the download count - so this would be 1m
            useCredentials: true
          {{ end }}

          {{ if .Values.seerrbridge.enabled }}
          - name: "SeerrBridge (backend)"
            icon: "fas fa-bridge"
            subtitle: "Use DMM from Overseerr (backend)"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-seerrbridge-backend.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute   

          - name: "SeerrBridge BridgeBoard"
            icon: "fas fa-bridge"
            subtitle: "SeerrBridge BridgeBoard"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-seerrbridge.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute                        
          {{ end }}

          {{ if .Values.zurg.enabled }}
          - name: "Zurg"
            logo: "assets/png/real-debrid.png"
            subtitle: "Manage RealDebrid Mount"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-zurg.{{ .Values.dns_domain }}/http/"
            target: "_blank" # optional html tag target attribute
          {{ end }}                                      

      - name: "Tools"
        icon: "fas fa-toolbox"
        items:

          {{ if .Values.actual.enabled }}
          - name: "Actual Server"
            logo: "assets/png/actual-budget.png"
            subtitle: "Actual Budget"
            tag: "public"
            tagstyle: "is-danger"
            {{ if .Values.actualcustomdomain.enabled }}
            url: https://{{ .Values.actualcustomdomain.cname | lower }}
            {{ else }}
            url: "https://{{ .Release.Name }}-actual.{{ .Values.dns_domain }}"
            {{ end }}            
            target: "_blank" # optional html tag target attribute
          {{ end }}   

          {{ if .Values.blueskypds.enabled }}
          - name: "Bluesky PDS"
            icon: "fas fa-sun"
            subtitle: "Bluesky Personal Data Server"
            tag: "public"
            tagstyle: "is-danger"
            {{- if .Values.blueskypdscustomdomain.enabled }}
            url: "https://{{ .Values.blueskypdscustomdomain.cname | lower }}"
            {{- else }}
            url: "https://{{ .Release.Name }}-bluesky-pds.{{ .Values.dns_domain }}"
            {{- end }}
            target: "_blank" # optional html tag target attribute

          - name: "BlueSky PDS CLI"
            icon: "fas fa-sun"
            subtitle: "Bluesky PDS CLI Admin"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-bluesky-pds-cli.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }} 

          {{ if .Values.codeserver.enabled }}
          - name: "Code Server"
            logo: "assets/png/code-server.png"
            subtitle: "Code Server"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-codeserver.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}  

          {{ if .Values.elfbot.enabled }}
          - name: "ElfBot"
            # icon: "fas fa-robot"
            logo: "assets/png/elfbot.png"
            subtitle: "Manage Apps"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-elfbot.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          - name: "ElfTerm"
            icon: "fas fa-terminal"
            subtitle: "Enter the ElfTerm.. 🪄"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-elfterm.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute            

          - name: "FileBrowser"
            logo: "assets/png/filebrowser.png"
            subtitle: "Manage Files"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-filebrowser.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute

          {{ if .Values.gluetun.enabled }}
          - name: "Gluetun"
            icon: "fas fa-lock"
            subtitle: "Configure your BYO VPN"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-kubernetes.{{ .Values.dns_domain }}/#/configmap/aa-{{ .Release.Name }}/gluetun-config?namespace=aa-{{ .Release.Name }}"
            target: "_blank" # optional html tag target attribute
          {{ end }} 

          {{ if .Values.gotify.enabled }}
          - name: "Gotify"
            logo: "assets/png/gotify.png"
            subtitle: "Gotify"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-gotify.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.gotosocial.enabled }}
          - name: "GoToSocial"
            logo: "assets/png/gotosocial.png"
            subtitle: "Federated Microblogging"
            tag: "public"
            tagstyle: "is-danger"
            {{- if .Values.gotosocialcustomdomain.enabled }}
            url: "https://{{ .Values.gotosocialcustomdomain.cname | lower }}"
            {{- else }}
            url: "https://{{ .Release.Name }}-gotosocial.{{ .Values.dns_domain }}"
            {{- end }}
            target: "_blank" # optional html tag target attribute

          - name: "GoToSocial CLI"
            logo: "assets/png/gotosocial.png"
            subtitle: "GoToSocial CLI Admin"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-gotosocial-cli.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}     

          {{ if .Values.homepage.enabled }}
          - name: "Homepage"
            logo: "assets/png/homepage.png"
            subtitle: "Beautiful Homepage"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-homepage.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}        

          - name: "Kubernetes Dashboard"
            logo: "assets/png/kubernetes.png"
            subtitle: "Kubernetes Resources"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-kubernetes.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute

          {{ if .Values.miniflux.enabled }}
          - name: "Miniflux"
            logo: "assets/png/miniflux.png"
            subtitle: "Minimalistic RSS reader"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-miniflux.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.nightscout.enabled }}
          - name: "Nightscout"
            logo: "assets/png/nightscout.png"
            subtitle: "Manage CGM Data"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-nightscout.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}              

          {{ if .Values.pairdrop.enabled }}
          - name: "Pairdrop"
            icon: "fas fa-lock"
            subtitle: "Private Airdrop"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-pairdrop.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.peertube.enabled }}
          - name: "PeerTube"
            logo: "assets/png/peertube.png"
            subtitle: "PeerTube"
            tag: "public"
            tagstyle: "is-danger"
            {{ if .Values.peertubecustomdomain.enabled }}
            url: https://{{ .Values.peertubecustomdomain.cname | lower }}
            {{ else }}
            url: "https://{{ .Release.Name }}-peertube.{{ .Values.dns_domain }}"
            {{ end }}
            target: "_blank" # optional html tag target attribute

          - name: "PeerTube CLI"
            icon: "fas fa-terminal"
            subtitle: "PeerTube CLI"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-peertube-cli.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute            
          {{ end }}           

          {{ if .Values.pgadmin.enabled }}
          - name: "PGAdmin"
            logo: "assets/png/pgadmin.png"
            subtitle: "PostgreSQL Admin"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-pgadmin.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}   

          {{ if .Values.privatebin.enabled }}
          - name: "Privatebin"
            logo: "assets/png/privatebin.png"
            subtitle: "Minimalistic pastebin"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-privatebin.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.puter.enabled }}
          - name: "Puter"
            logo: "assets/png/puter.png"
            subtitle: "Remotely compute"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-puter.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}   

          {{ if .Values.redisinsight.enabled }}
          - name: "RedisInsight"
            logo: "assets/png/redisinsight.png"
            subtitle: "Redis UI"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-redisinsight.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}             

          {{ if .Values.thelounge.enabled }}
          - name: "The Lounge"
            logo: "assets/png/thelounge.png"
            subtitle: "IRC client"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-thelounge.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.uptimekuma.enabled }}
          - name: "Uptime Kuma"
            logo: "assets/png/uptime-kuma.png"
            subtitle: "Uptime Monitor"
            tag: "public"
            tagstyle: "is-danger"
            {{- if .Values.uptimekumacustomdomain.enabled }}
            url: "https://{{ .Values.uptimekumacustomdomain.cname | lower }}"
            {{- else }}
            url: "https://{{ .Release.Name }}-uptime-kuma.{{ .Values.dns_domain }}"
            {{- end }}
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.vaultwarden.enabled }}
          - name: "Vaultwarden"
            logo: "assets/png/vaultwarden.png"
            subtitle: "Securely manage passwords / secrets"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-vaultwarden.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}
       
          {{ if .Values.wallabag.enabled }}
          - name: "Wallabag"
            logo: "assets/png/wallabag.png"
            subtitle: "Read it Later"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-wallabag.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}

          {{ if .Values.wger.enabled }}
          - name: "Wger"
            logo: "assets/png/wger.png"
            subtitle: "Track fitness and nutrition"
            tag: "public"
            tagstyle: "is-danger"
            {{ if .Values.wgercustomdomain.enabled }}
            url: https://{{ .Values.wgercustomdomain.cname | lower }}
            {{ else }}
            url: "https://{{ .Release.Name }}-wger.{{ .Values.dns_domain }}"
            {{ end }}
            target: "_blank" # optional html tag target attribute
          {{ end }}          


      - name: "Rclone"
        icon: "fas fa-truck"
        items:

          {{ if .Values.rclonealldebrid.enabled }}
          - name: "AllDebrid"
            logo: "assets/png/rclone.png"
            subtitle: "AllDebrid (WebDAV)"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-alldebrid.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}  

          {{ if .Values.rclonedebridlink.enabled }}
          - name: "Debrid-Link"
            logo: "assets/png/rclone.png"
            subtitle: "Debrid-Link (WebDAV)"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-debridlink.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }} 

          {{ if .Values.rclonepremiumize.enabled }}
          - name: "Premiumize"
            logo: "assets/png/rclone.png"
            subtitle: "Premiumize (WebDAV)"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-premiumize.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}             

          - name: "Rclone FM"
            logo: "assets/png/rclone.png"
            subtitle: "Rclone File Manager"
            tag: "protected"
            tagstyle: "is-success"
            {{ if .Values.rclonefm.enabled }}
            url: "https://{{ .Release.Name }}-rclonefm.{{ .Values.dns_domain }}"
            {{ else }}
            url: https://store.{{ .Values.dns_domain }}/product/rclonefm
            {{ end }}
            target: "_blank" # optional html tag target attribute

          - name: "Rclone Remotes"
            logo: "assets/png/rclone.png"
            subtitle: "Rclone remotes"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-filebrowser.{{ .Values.dns_domain }}/files/config/rclone/rclone.conf"
            target: "_blank" # optional html tag target attribute

          - name: "Rclone UI"
            logo: "assets/png/rclone.png"
            subtitle: "Rclone UI"
            tag: "protected"
            tagstyle: "is-success"
            {{ if .Values.rcloneui.enabled }}
            url: "https://{{ .Release.Name }}-rcloneui.{{ .Values.dns_domain }}"
            {{ else }}
            url: https://store.{{ .Values.dns_domain }}/product/rcloneui
            {{ end }}
            target: "_blank" # optional html tag target attribute

          {{ if .Values.rclonetorbox.enabled }}
          - name: "TorBox"
            logo: "assets/png/rclone.png"
            subtitle: "TorBox (WebDAV)"
            tag: "protected"
            tagstyle: "is-success"
            url: "https://{{ .Release.Name }}-torbox.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}  

          {{ if .Values.webdav.enabled }}
          - name: "WebDAV"
            logo: "assets/png/rclone.png"
            subtitle: "WebDAV"
            tag: "public"
            tagstyle: "is-danger"
            url: "https://{{ .Release.Name }}-webdav.{{ .Values.dns_domain }}"
            target: "_blank" # optional html tag target attribute
          {{ end }}
                                     

  custom.css: |
    @charset "UTF-8";

    /* raleway-regular - latin */
    @font-face {
      font-family: 'Raleway';
      font-style: normal;
      font-weight: 400;
      src: url('./fonts/raleway-v22-latin-regular.eot'); /* IE9 Compat Modes */
      src: local(''),
          url('./fonts/raleway-v22-latin-regular.eot?#iefix') format('embedded-opentype'), /* IE6-IE8 */
          url('./fonts/raleway-v22-latin-regular.woff2') format('woff2'), /* Super Modern Browsers */
          url('./fonts/raleway-v22-latin-regular.woff') format('woff'), /* Modern Browsers */
          url('./fonts/raleway-v22-latin-regular.ttf') format('truetype'), /* Safari, Android, iOS */
          url('./fonts/raleway-v22-latin-regular.svg#Raleway') format('svg'); /* Legacy iOS */
    }

    /* lato-regular - latin */
    @font-face {
      font-family: 'Lato';
      font-style: normal;
      font-weight: 400;
      src: url('./fonts/lato-v20-latin-regular.eot'); /* IE9 Compat Modes */
      src: local(''),
          url('./fonts/lato-v20-latin-regular.eot?#iefix') format('embedded-opentype'), /* IE6-IE8 */
          url('./fonts/lato-v20-latin-regular.woff2') format('woff2'), /* Super Modern Browsers */
          url('./fonts/lato-v20-latin-regular.woff') format('woff'), /* Modern Browsers */
          url('./fonts/lato-v20-latin-regular.ttf') format('truetype'), /* Safari, Android, iOS */
          url('./fonts/lato-v20-latin-regular.svg#Lato') format('svg'); /* Legacy iOS */
    }

    /* lato-700 - latin */
    @font-face {
      font-family: 'Lato';
      font-style: normal;
      font-weight: 700;
      src: url('./fonts/lato-v20-latin-700.eot'); /* IE9 Compat Modes */
      src: local(''),
          url('./fonts/lato-v20-latin-700.eot?#iefix') format('embedded-opentype'), /* IE6-IE8 */
          url('./fonts/lato-v20-latin-700.woff2') format('woff2'), /* Super Modern Browsers */
          url('./fonts/lato-v20-latin-700.woff') format('woff'), /* Modern Browsers */
          url('./fonts/lato-v20-latin-700.ttf') format('truetype'), /* Safari, Android, iOS */
          url('./fonts/lato-v20-latin-700.svg#Lato') format('svg'); /* Legacy iOS */
    }

    /* lato-900 - latin */
    @font-face {
      font-family: 'Lato';
      font-style: normal;
      font-weight: 900;
      src: url('./fonts/lato-v20-latin-900.eot'); /* IE9 Compat Modes */
      src: local(''),
          url('./fonts/lato-v20-latin-900.eot?#iefix') format('embedded-opentype'), /* IE6-IE8 */
          url('./fonts/lato-v20-latin-900.woff2') format('woff2'), /* Super Modern Browsers */
          url('./fonts/lato-v20-latin-900.woff') format('woff'), /* Modern Browsers */
          url('./fonts/lato-v20-latin-900.ttf') format('truetype'), /* Safari, Android, iOS */
          url('./fonts/lato-v20-latin-900.svg#Lato') format('svg'); /* Legacy iOS */
    }

    /* Sets the group title to be a bolder font */
    .group-title {
      font-family: Lato;
      font-weight: 800;
    }

    /* Changes the card title to be a bolder font */
    .title {
      font-weight: 700;
    }

    /* Changes the subtitle to be a bolder font */
    .subtitle {
      font-weight: 500;
    }

    /* Adds spacing to the bottom of cards */
    body #app .card-content {
      margin-bottom: 0.75rem;
    }

    /*Adds more border radius to the cards */
    body .layout-vertical .column div:first-of-type .card {
      border-radius: 1rem;
    }
    body .layout-vertical .card {
      border-radius: 1rem;
    }
    .card {
        border-radius: 1rem;
    }
    body .layout-vertical .column div:last-of-type .card { 
        border-radius: 1rem; 
    }

    /* Changes Header Opacity */
    body #bighead .first-line {
      background-color: #50762E;
    }
    body #bighead .navbar {
      background-color: #36511F;
    }

    body .search-bar input {
        opacity: 20%;
    }

    body #bighead .navbar a:focus, body #bighead .navbar a:hover {
      background-color: #50762E;
      color: var(--link-hover);
    }    